const LoginButton = ({ buttonText }) => (
  <button type="submit" className="w-full py-2 mt-4 rounded-full bg-buff text-eggplant focus:outline-none">
    {buttonText}
  </button>
);

export default LoginButton;